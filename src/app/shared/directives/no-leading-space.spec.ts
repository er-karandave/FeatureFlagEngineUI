import { NoLeadingSpace } from './no-leading-space';

describe('NoLeadingSpace', () => {
  it('should create an instance', () => {
    const directive = new NoLeadingSpace();
    expect(directive).toBeTruthy();
  });
});
